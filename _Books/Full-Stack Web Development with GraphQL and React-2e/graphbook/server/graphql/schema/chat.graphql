type Chat {
  id: Int
  messages: [Message]
  users: [User]
}

input ChatInput {
  users: [Int]
}

extend type RootQuery {
  chats: [Chat]
  chat(chatId: Int): Chat
}

extend type RootMutation {
  addChat(chat: ChatInput!): Chat
}
